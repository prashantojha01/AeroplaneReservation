package com.example.AeroplaneReservation.exception;

import com.example.AeroplaneReservation.dto.Flight;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;

//@ControllerAdvice
public class GeneralizedExxceptionMapper{
   // @ExceptionHandler(DoNotFoundException.class)
    //public ResponseEntity<Error> notFoundException(final DoNotFoundException e) {
    //    return new Error(e.getErrorCode(), e.getErrorMessage());
   // }

}
